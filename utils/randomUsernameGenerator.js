
module.exports = (firstName, lastName) => {

    const fullName = `${this.firstName}${this.lastName}`;
    const count = maxUsernameLength - fullName.length;

    if (count >= 20) {
        return fullName;
    }
}